# Install with: pip install -r requirements.txt

# Core dependencies
requests>=2.28.0
python-telegram-bot>=20.0
aiohttp>=3.8.0

# HTML parsing for SMS portal
beautifulsoup4>=4.11.0
lxml>=4.9.0

# Signature generation (choose one)
# Option 1: SignerPy (original, preferred)
# SignerPy>=0.11.4

# Option 2: tiktok-signer (alternative)
tiktok-signer>=1.3.1

# Utilities
pytz>=2022.1
